// ============================================================
// ipc/mod.rs — IPC module (Named Pipe server + handlers + types)
// ============================================================

pub mod handlers;
pub mod server;
pub mod types;
